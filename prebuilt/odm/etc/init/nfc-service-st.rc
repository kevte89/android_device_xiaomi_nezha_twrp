service nfc_hal_service /odm/bin/hw/android.hardware.nfc-service-st
    class hal
    user nfc
    group nfc
    # AIDL interfaces
    interface aidl android.hardware.nfc.INfc/default
    # don't auto restart
    oneshot
    # start only when requested
    disabled